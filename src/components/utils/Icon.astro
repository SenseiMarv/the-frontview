---
export interface Props {
  iconSet?: string;
  name: string;
  classString?: string;
}

const { iconSet = "fluent-emoji", name, classString = "" } = Astro.props;
---

<iconify-icon
  icon={`${iconSet}:${name}`}
  class={`${classString} inline-flex align-bottom text-2xl`}
/>
