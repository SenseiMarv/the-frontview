---
import { Icon } from "astro-icon";

export interface Props {
  iconSet?: string;
  name: string;
  classString?: string;
}

const { iconSet = "fluent-emoji", name, classString = "" } = Astro.props;
---

<Icon name={`${iconSet}:${name}`} class={`${classString} inline-flex w-6`} />
