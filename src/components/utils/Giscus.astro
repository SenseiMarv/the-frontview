<script is:inline>
  const sendGiscusMessage = (/** @type any */ message) => {
    const iframe = document.querySelector("iframe.giscus-frame");
    if (!iframe || !(iframe instanceof HTMLIFrameElement)) return;
    iframe.contentWindow?.postMessage(
      { giscus: message },
      "https://giscus.app"
    );
  };

  const observer = new MutationObserver((mutations) => {
    mutations.forEach((mutation) => {
      if (
        mutation.type === "attributes" &&
        mutation.attributeName === "data-theme" &&
        mutation.target instanceof HTMLElement
      ) {
        const theme =
          mutation.target.dataset.theme === "light"
            ? "light"
            : "transparent_dark";

        sendGiscusMessage({
          setConfig: {
            theme,
          },
        });
      }
    });
  });
  observer.observe(document.documentElement, { attributes: true });
</script>
<script
  src="https://giscus.app/client.js"
  data-repo="SenseiMarv/the-frontview"
  data-repo-id="R_kgDOH7UC1w"
  data-category="Announcements"
  data-category-id="DIC_kwDOH7UC184CRz3Q"
  data-mapping="pathname"
  data-strict="0"
  data-reactions-enabled="1"
  data-emit-metadata="0"
  data-input-position="top"
  data-theme="transparent_dark"
  data-lang="en"
  data-loading="lazy"
  crossorigin="anonymous"
  async
  is:inline
></script>
